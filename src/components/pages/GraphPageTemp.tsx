import GraphInputModal from "../GraphInputModal";

interface GraphPageProps {}

const GraphPageTemp: React.FC<GraphPageProps> = (
    {}: GraphPageProps
) => {
    return (
        <div>
            <GraphInputModal />
        </div>
    )
}

export default GraphPageTemp;
